@AGENTS.md

# 청약핏

- 청약·공공임대 공고 판정 서비스. 설계는 docs/DESIGN.md — 새 기능 전에 먼저 읽을 것.
- **스크래핑 금지.** 공고는 공공데이터포털 공식 API(청약홈·마이홈·LH·HUG)만. API 없는 기관은 사실만 옮기고 원문 링크.
- 판정은 세 값(pass/fail/unknown). 모르는 값을 pass·fail로 치지 말 것. 부동산 기준처럼 총자산으로 단정할 수 없는 건 unknown.
- 해마다 바뀌는 숫자는 src/lib/rules/standards.ts에만. 출처를 주석에 남길 것.
- 프로필은 localStorage에만 저장한다. 서버로 개인정보를 보내는 기능은 따로 설계·동의부터.
- 규칙 계산을 「AI」라고 부르지 않는다.
- 예시 데이터 단계(SITE.sampleData=true)에선 공고 화면(/notice)에 예시 표시·noindex. 가이드·홈은 실제 법령 기준이라 색인한다.
- 가이드 숫자는 손으로 적지 않는다 — standards.ts·criteria.ts에서 계산. 확인 안 된 값은 「확인 중」으로 표시.
- UI는 README 「디자인 규격」 토큰만(버튼 size·variant, StatusBadge, t-* 글자 크기). className으로 버튼 높이·패딩을 덮지 않는다.
- 디자인은 README 「불 켜진 창」: 흰 바탕·Pretendard·군청 하나·괘선. 창 1칸 = 공고 1건(데이터 없는 창 금지). 검은 배경·세리프·폰 목업·파스텔 타일·그라디언트·색 eyebrow·「~해 드려요」 남발은 쓰지 않는다(「어둡고 어렵다」→「AI 티」 피드백, 2026-09-25). 가독성·말투 규칙(색 하나에 뜻 하나, 데스크탑 15px 이상, 줄표·꺾쇠·가운뎃점 제한, 버튼 이름 고정)도 README에 있다(「안 보인다·AI 티」 피드백, 2026-09-26).
- 조사는 lib/josa.ts, 지역명은 lib/place.ts placeText. 문장 틀에 값을 끼울 때 「이(가)」·중복 단어가 나오지 않는지 확인.
- 헤드라인·짧은 카피 줄바꿈은 br("… | …")로 말 쉬는 자리에서만. 긴 문장을 통째로 묶지 말 것(좁은 화면에서 낱말 중간이 잘린다).
- 판정 날짜(D-day)는 브라우저에서만 계산한다(서버 UTC와 어긋남 방지).
